package com.example.configuration;

import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;


public class MvcConfiguration implements WebMvcConfigurer {

    //todo: 1. Add MvcConfiguration. (hint: don't forget about annotations).
    // You need method addViewController which will register your views
    // You need ViewResolver bean with info where your view placed (WEB-INF...)
}
