package com.example.controller;

public class HomeController {

    //todo: 4. Complete the HomeController. It should contain the following:
    // - GET endpoint '/home' that returns a home view.
    // - GET endpoint '/users' with optional query param 'id'. This endpoint should pass info to the userService
    // and return view with a model from userService.
}
